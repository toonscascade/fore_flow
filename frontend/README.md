# Foreflow Frontend

Redesigned minimalist navy + colourful UI for the Network Threat Foreflow platform.

## Run

```bash
npm install
npm run dev
```

The existing API contract remains unchanged and Vite continues to proxy `/api` to the FastAPI backend on port `8000`.
